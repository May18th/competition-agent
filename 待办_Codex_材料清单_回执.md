# 回执：材料搜集清单（给 Codex）后端部分已闭环

> 对应《还需要搜集的材料清单（给 Codex）》。以下为后端本次改动与验证结果。

## 一、P1 知识库标题/内容错位 —— 已修（5 个文件）

| 文件 | 问题 | 修法 |
|---|---|---|
| `data/iCAN.txt` | `## 申报书章节` 里写的是三个赛道说明 | 拆成 `## 赛道设置`，并补真正的 `## 申报书章节`（9 节，取自 event_requirements.json 官方结构） |
| `data/国创.txt` | `## 评分标准` 写的是选题要求（无百分比）；`## 申报书章节` 写的是结题材料 | 改 `## 立项标准` + `## 结题要求`，并把「和比赛的区别」改成 `## 与其他比赛的区别`，补 `## 常见扣分点` |
| `data/RoboMaster.txt` | 出现两个 `## 评分标准`，第二个其实是章节列表 | 第二个改 `## 申报书章节` |
| `data/小挑.txt` | 无 `## 常见扣分点` | 补 5 条；「和大挑的区别」从 `## 偏好方向` 改 `## 与大挑的区别` |
| `data/蓝桥杯.txt` | 评分权重是「通用参考侧重」非官方 | 把免责说明写得更明确：不公开权重、上表非官方数据、以当届通知为准 |

配套改了 `competition_agents.py`：

- `parse_knowledge` 新增 `requirements` 字段，捕获「立项/选题/结题/赛道/要求」类章节（之前这些内容会被丢掉）。
- `rule_parser_agent` 的 prompt 里新增一行「立项/结题/赛道等其他要求」，把这些内容喂给规则解析。
- `_join` 从「只取第一个匹配」改成「拼接所有匹配」，这样国创的「立项标准 + 结题要求」两块都能注入。
- `preference` 关键词补了「区别」，让「与其他比赛的区别 / 与大挑的区别」仍能被注入。

## 二、P0 真实获奖范文入库 —— 后端已就绪（无需再改接口）

真实获奖稿有两条入库路径，都会**绕过「<75 分归材料」的质量门**，直接 `type=范文、score=90`：

1. **JSON 接口（推荐）**：`POST /api/kb/add`，body 显式传
   `{content, tags:["项目简介", ...], type:"范文", score:90, source:"官方获奖作品", summary:"..."}`
   —— `kb_add` 早就支持显式 type/score，`score=None` 才走自动打分，所以显式传就绕过。
2. **文件上传**：`POST /api/upload_pdf` 或 `/api/upload_file`，form 加 `purpose=fanwen`。
   本次已接线：`_record_upload(..., force_fanwen=True)` 会强制归范文、score 90。

> 注意：真正「搜集真实获奖稿」是队友的活，后端只负责「收得进来、不进错类型」。收进来后验证：`SELECT type, COUNT(*) FROM kb_samples GROUP BY type`，范文应显著增加，且 source 不再是「AI 撰写」。

## 三、根因修复（防回归）

`import_official_kb.py` 的 `_classify` 有三处会复现上述错位，已修：

- 「评审」太贪：会把 RoboMaster 的「申报书…（赛前技术评审报告）」误判成「评分标准」→ 改为只认「评分/打分」。
- 「立项标准 / 结题要求 / 赛道设置」被并进「评分标准 / 申报书章节」→ 拆成独立分类。
- 「区别」从「偏好方向」改独立为「与其他比赛的区别」。

⚠️ 但 `import_official_kb.py` 是本地一次性转换脚本，**重新跑仍会覆盖**我手工补的 iCAN 申报书章节、国创/小挑常见扣分点。建议：不要再跑这个脚本，或把这些手工补充固化进源文件。它不在主仓库 git 里，不会随 commit 上线。

## 四、验证结果（真实跑通）

- `py_compile` app.py / competition_agents.py / import_official_kb.py 全过；主仓库 `ast.parse` 语法检查过。
- `get_competition_knowledge_structured` 实测：
  - iCAN → scoring=创新性30/技术实现30…（AI 应用挑战赛）、sections=9 节真实章节、requirements=赛道设置、penalty 有。
  - 国创 → scoring=空（正确，无百分比）、sections=空（正确，无传统申报书章节）、requirements=立项标准+结题要求、penalty 有。
  - RoboMaster → scoring=技术评审40/对抗60、sections=8 节章节、penalty 有。
  - 小挑 → scoring/sections/penalty 齐，requirements 空。
  - 蓝桥杯 → scoring 带明确「非官方」免责。
- 大挑/小挑 匹配未回归：`…课外学术科技作品竞赛（大挑）→挑战杯`、`…创业计划竞赛（小挑）→小挑` 正确。

## 五、需要你拍板的两件事

1. **iCAN 评分标准两处来源冲突**：队友原始文件写「评分标准（AI 应用挑战赛）= 创新性30 / 技术实现30 / 实用价值20 / 用户体验10 / 展示效果10」；`event_requirements.json` 写「创新性30 / 实用性25 / 技术难度20 / 团队展示15 / 社会价值10」。两者不一样。本次知识库 txt **保留了队友原始口径（并标注 AI 应用挑战赛）**，但 `event_requirements.json` 那份仍是另一套。需要确认哪套是当届官方口径，或明确「一个是 AI 挑战赛专项、一个是通用」。

2. **行业数据没恢复**：工作区里有一批更全的知识库版本（含 `## 行业数据` 且带来源，如 iCAN 第19届 36521 个项目等），今天 13:57 那次 `import_official_kb.py` 重跑把它们覆盖没了。本次只修了「标题/内容错位」，没把行业数据恢复回来——因为要逐条核实来源、防假数据。要不要恢复，作为独立任务再排。

## 六、没做的（按清单明确跳过/留给队友）

- 真实获奖范文的**搜集**：队友上传，后端已具备接收能力。
- P2 答辩真题库：nice-to-have，未做。
- P3 学校模板：跳过，靠用户上传。
- 查重/降重：无知网接口，做不了真查重。
