# 后端待办清单（负责人：Codex）

> 目标文件：`app.py`、`competition_agents.py`、`data/*.txt`
> **请不要改 `index.html` 和 `static/`**（前端由豆包和我负责）。
> 修改 Python 代码后**必须重启 Flask**，`run.py` 里 `debug=False`，不会热重载。

## 〇、先说三件已经发生的事，避免重复劳动或互相覆盖

1. **我（阿渡）越界改过一次后端，请保留不要覆盖回去**
   执行脚本：`wb_fix_writer_quality.py`（幂等，可重跑），改了 `competition_agents.py` 三处：
   - 简洁版 writer 删掉 `competitor_analysis / business_model / risk_analysis / tech_solution` 这 4 个**流水线从未生成的空输入槽**，换成实际存在的 `one_liner`；
   - 简洁版去掉「总共500字左右」「不要写商业模式、技术方案」的自我削弱文案，改为 800-1000 字 + 硬性要求每节必须有具体场景/数字/技术名词；
   - 深度版 writer 的「用户已经上传草稿」改成条件判断（检测 `申报书草稿：` 标记），并且**重写时带上上一版全文 + `judge_feedback`**（原来是蒙眼重写一遍）。
   验证方式：`python -c "import competition_agents"` 能过，`inspect.getsource` 里旧字符串已消失、`has_draft`/`judge_feedback` 已出现。

2. **目录下已有 30 个 `fix_*.py` 补丁脚本是你写的**，其中 `fix_charts.py` / `fix_responsive.py` / `fix_perf.py` 我已经替你跑完了（跑之前它们从没被执行过，文件 mtime 一直停在 03:58:53）。跑完发现 `fix_responsive.py` 三处替换只成功一处——字符串 replace 对不上时会静默跳过还照样打印 ✅，建议以后每个补丁结尾打印**替换命中次数**。

3. **端口冲突**：今天 04:41~04:52 出现过两个进程同时监听 8080（PID 23160 与 57056），其中一个跑的是旧代码。现在只剩一个 PID 25040（我起的）。**请改完代码后先确认 8080 上只有一个进程**，别重复 `python run.py`。

---

## 一、待办（按优先级）

### P1｜简洁版有 7 个「注册了但从没执行」的孤儿节点  ✅ 已完成（2026-09-25）

> 已按方案 A 落地：`defense / ppt / speech` 已接进链路（`judge → proposal_analysis → defense → ppt → speech → END`），简洁版现在也能出答辩材料。

用图接口实测的结果（`fast_app.get_graph()` 做可达性遍历）：

```
简洁版实际链路：__start__ → rule_parser → similarity_checker → one_liner → writer → judge → proposal_analysis → __end__
孤儿节点：analysis, plan, social, summary, defense, ppt, speech
```

影响：这 7 个节点在 `add_node` 里注册了（563-573 行一带），但 `add_edge` 没接上，永远不跑。于是简洁版永远拿不到：实施计划、社会价值、项目摘要、答辩问题、PPT 大纲、演讲稿。而首页「Agent 流程条」宣传的「答辩预测 / PPT 大纲」对简洁版是不成立的。

**请你决定并落地**（二选一，我倾向 A）：
- A：把 `one_liner/writer` 之后按 `judge → proposal_analysis → defense → ppt → speech → END` 接完，让简洁版也出答辩材料；
- B：若为了保持「简洁版快」，就把这 7 个 `add_node` 删掉，避免误导；同时要同步告诉我，我把首页流程条改成只显示前四项。

### P2｜深度版四个分析节点仍然串行，白等 6 次调用  ✅ 已完成（2026-09-25）

> 已改并行：`competitor / business / risk / tech` 四节点从 START 直接扇出，各自返回增量 dict。

```
similarity_checker → competitor → business → risk → tech → plan → social → summary → writer
```

字段级依赖扫描结论：这七个节点**清一色只读 `idea`**，互不依赖，没有理由串行。环境是 langgraph 1.2.12，原生支持扇出/扇入。

改并行时请注意一个坑：现在每个 Agent 都是「改同一个 `state` 对象再 `return state`」的写法。并行分支下这样写会互相覆盖。**建议改成每个节点只返回自己的增量 dict**，或给 state 字段配 reducer，否则并行会丢数据。

### P3｜简洁版没有迭代

深度版有 `should_iterate`（第 553 行）条件边，低分会回 writer 重写；简洁版完全没有回边，分数再低也不返工。
若保持简洁版「快」的定位可以不做，但请在代码里写明这是有意设计。

### P4｜评分机制有两个问题  ✅ 已完成（2026-09-25）

> prompt 已去掉「60-85 分」写死，改为「每项 0-100 + 拉开差距」；抓分改为分维度加权 + 结构化 `SCORE:` 行 + `clamp` 到 0-100。

1. `judge_agent`（第 439 行）在 prompt 里写死「总分（60-85分之间，不要打太低」——分数是被约束出来的，不是评出来的，用户看到的「80分」含金量存疑。
2. 分数靠 `re.search(r'(\d+)\s*分', response.content)` 抓**全文第一个**「数字+分」，模型输出里只要先出现别的「xx分」就会抓错，兜底值 60。
建议：让模型在第一行单独输出 `SCORE: 78` 这种结构化行，正则改成锚定行首 + 行尾，兜底逻辑也一并校正。

### P5｜知识库只覆盖了 11 个赛事里的 4 个

`data/` 下只有：`iCAN大赛.txt`(460B)、`互联网+大赛.txt`(585B)、`国创项目.txt`(493B)、`挑战杯.txt`(479B)。
下拉框里还有 7 个赛事查不到任何资料，`get_competition_knowledge` 直接返回空字符串，评分标准全靠模型编。
每个文件建议至少包含：赛制阶段、评分标准（分值占比）、字数/格式要求、常见扣分项。这部分需要真实官方材料，**我这边没有可靠来源，请你接时应配 FetchDoc 或让用户提供**。

### P6｜`app.py` 里还躺着一份从不生效的内嵌 HTML 副本  ✅ 已完成（2026-09-25）

> 内嵌 HTML 已删除，`app.py` 现约 818 行 / 36KB，仅保留一行正常读取 `index.html`。

`app.py` 第 840 行是 `@app.route('/')` → `open('index.html')`，所以生效的只有 `index.html`。但文件里另外嵌了一整份 HTML（约 6 万字符），之前两边都改错过目标。
建议把这整段删掉——但在删之前请先确认没有别的路由在引用它，删完务必再跑一次首页自检（返回字节数应等于 `index.html` 的字节数）。

### P7｜导出链路已实测通过，但没人做过端到端真跑

我压测过：`POST /api/export_zip` 传 `parsed_rules`/`proposal`/`proposal_analysis` 三个字段，返回 200 + `application/zip` + 103562 字节，解包出 `01_赛事规则解析报告.docx`、`03_项目申报书全文.docx`、`04_申报书诊断分析.docx`，没传的字段（`similarity_report`）被正确跳过。**这条链路是通的，不用再核。**

但 `proposal_analysis` 是否真能拿到 AI 内容、新 prompt 效果如何，**必须真跑一次生成才能确认**（会消耗 DeepSeek 额度）。你改完后端重启后，建议用一组固定样例自测并记录三列：评委分 / 实际字数 / 端到端耗时，别再凭感觉调 prompt。

---

## 二、边界与协作约定

- 前端（`index.html`、`static/`）归豆包和我，**请不要改**，也别在 `app.py` 里再生成一份 HTML。
- 我在 `static/` 下新增了 `marked.min.js`(39903B) 和 `echarts.min.js`(1024740B)，用于 CDN 不可达时的本地兜底；Flask 默认静态路由已经能正常返回这两个文件（实测 200），**不要另加 `/static` 路由覆盖它**。
- 项目根目录现在多了三个我建的文件，别删：`wb_fix_writer_quality.py`（改后端的补丁脚本）、`待办_前端.md`、`待办_后端.md`。
