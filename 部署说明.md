# 部署说明（为什么 Vercel 部署失败，以及该怎么上线）

> 更新时间：2026-09-25　｜　结论先说：**这次构建失败不是代码写错了，是平台没选对。** 本项目是常驻进程的 Flask 应用，Vercel 是 Serverless 平台，两者不合。

---

## 一、日志逐条解读

你贴的那段日志里，有三行是决定性的：

```
Running "npm run build" ... "build": "next build"
Installing dependencies...
npm error network request to https://registry.npmjs.org/react-json-view failed, reason: socket hang up
Error: Command "npm run build" exited with 1
```

| 日志 | 含义 |
|---|---|
| `"build": "next build"` | Vercel 认为这是 **Next.js 项目**，所以跑去执行 `next build` |
| `Installing dependencies` + `registry.npmjs.org` | 它在**装 Node 依赖**（npm），说明走的是 Node.js runtime |
| `socket hang up` / `ECONNRESET` | Vercel 的构建机（美国 iad1）连 npm 官方源被掐断——这是**结果**，不是原因 |

**根因：仓库里一个 Node 文件都没有。**

已核对（`git ls-files` + 根目录 `ls`）：`package.json`、`next.config.js`、`vercel.json` **全部不存在**，也没有 `node_modules`。
一个纯 Python 项目不可能凭空冒出 `next build` 这条命令——**是 Vercel 项目设置里的 Framework Preset 被固定成了 Next.js**（新建项目时手选过，或 Vercel 自动识别失败后固化了）。

**次要原因**：原来的 `requirements.txt` 带 UTF-8 BOM（`\ufeff` 开头）。就算 Vercel 认出是 Python，某些构建环节读这个也会出错。已修掉，并补全了缺失的依赖。

---

## 二、三条路线对比

| | Vercel | Render（推荐） | 轻量云服务器 / Railway |
|---|---|---|---|
| 能不能跑起来 | ❌ 基本不可能 | ✅ 可以 | ✅ 可以 |
| 长任务（几分钟的生成） | ❌ 免费版最长 60s 必超时 | ✅ 无硬性超时 | ✅ |
| 写文件（generated / uploads / 上传） | ❌ 文件系统只读，只有 `/tmp` 可写 | ✅ 可写 | ✅ |
| SQLite 持久化 | ❌ 写不进去 | ⚠️ 免费实例重启会丢 | ✅ 稳定 |
| 异步进度内存字典 | ❌ 多实例不共享，轮询必失联 | ✅ 单实例 | ✅ |
| 冷启动 | 快 | 免费版 15 分钟无请求会休眠，首次访问要等 30~50s | 无 |
| 费用 | 免费 | 免费 | ~10 元/月起 |

**推荐：Render。** 配置已经写好，理论上只需 3 步。

---

## 三、路线 A：Render（推荐）

仓库里已经有 `render.yaml` + `Procfile` + `wsgi.py` + `requirements.txt`，无需再改任何东西。

1. 打开 <https://render.com> → 右上角 **New +** → **Blueprint**
2. 连 GitHub，选 `May18th/competition-agent` 仓库 → Render 会自动读到 `render.yaml`
3. 在 **Environment** 里手动填一个环境变量（唯一要在网页上填的东西）：

   ```
   DEEPSEEK_API_KEY = sk-xxxxxxxxxxxxxxxx
   ```

4. 点 **Apply** → 等第一次构建（约 3~6 分钟，主要耗在装 langchain/matplotlib）→ 拿到 `.onrender.com` 域名

**几个已知坑：**

- **服务会休眠**：免费套餐 15 分钟没人访问就停，下次有人点开要等 30~50 秒才起来。答辩演示前请**自己先访问一次**把它唤醒。
- **中文字体**：`render.yaml` 的 `buildCommand` 里那句 `apt-get install -y fonts-noto-cjk` 就是装中文字体的。如果它报权限错，把这一行删掉即可——代价只是图表里的中文变成方框 □□□，其余功能正常。
- **历史记录会丢**：免费实例重启会清空磁盘，SQLite 里的生成历史不保证跨重启存在。要长期存就得换付费盘或外部数据库。
- **workers 必须是 1**：`Procfile` 里写死了 `--workers 1`。改成多进程会导致「提交任务」和「查进度」落到不同进程，进度条直接失联。

---

## 四、路线 B：轻量云服务器（腾讯云 / 阿里云，最稳）

适合要保证答辩当天不掉链子的情况。一台 2 核 2G 学生机就能跑。

```bash
# 1. 装依赖
sudo apt-get update
sudo apt-get install -y python3.11 python3.11-venv fonts-noto-cjk nginx

# 2. 拉代码
git clone https://github.com/May18th/competition-agent.git
cd competition-agent

# 3. 建虚拟环境并装包
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 4. 写密钥
echo "DEEPSEEK_API_KEY=sk-xxxxxxxx" > .env

# 5. 用 systemd 常驻（或者直接用 nohup）
nohup gunicorn wsgi:app --bind 0.0.0.0:8080 --workers 1 --threads 4 --timeout 600 > app.log 2>&1 &
```

然后 nginx 反代 8080 端口，配域名和 HTTPS 即可。

---

## 五、路线 C：Vercel —— 为什么不建议，以及硬要做要改什么

我已经在仓库里放了 `vercel.json` + `api/index.py`，加完之后 **Vercel 不会再跑去执行 `npm run build`**，构建这关能过。

**但运行时几乎必挂。** 四个死结，都不在 Node/Python 之争，而在架构：

| # | 卡点 | 位置 | 现象 |
|---|---|---|---|
| 1 | 只读文件系统 | `app.py:55` `os.makedirs('uploads')` 在项目根目录建目录 | Vercel 只有 `/tmp` 可写 → **import 阶段直接抛异常，全站 500** |
| 2 | SQLite 不可写 | `app.py:34` `competition.db` 写在 `__file__` 同级目录 | 历史记录写不进去 |
| 3 | 异步任务状态在内存 | `app.py:393` `tasks = {}`、`app.py:615` 后台线程 | Vercel 每次请求可能落不同实例 → 提交任务和轮询进度不在同一个进程 → 进度条永远查不到 |
| 4 | 任务超时 | 深度版一次生成要跑几分钟 | 免费版函数最长 60s → 直接被掐断 |

**如果一定要走 Vercel**，得先做这四处改造（**属于 `app.py` 核心逻辑，归 Codex 负责，我没有直接动**，已同步到 `待办_后端.md`）：

1. 把所有写盘路径改成优先落 `/tmp`，找不到就降级（`uploads` / `generated` / `tmp_export` / `competition.db`）
2. 把任务状态从内存字典换成外部存储（Upstash Redis 或数据库表）
3. 长任务改走 Vercel 的后台队列 / webhook 回调，不能在请求线程里跑完
4. 依赖瘦身（langchain + matplotlib 打包体积逼近 250MB 上限）

改完这四项的成本，比直接换 Render 高一个数量级。**建议：答辩演示用 本地 + cpolar（已有，零成本），公网长期访问用 Render。**

---

## 六、环境变量清单

云平台一律在自己的面板里配，**不要写进仓库**（`.env` 已被 `.gitignore` 忽略）。

| 变量 | 必填 | 说明 |
|---|---|---|
| `DEEPSEEK_API_KEY` | ✅ | DeepSeek 密钥，Agent 链要用 |
| `QUOTA_DAILY_LIMIT` | 可选 | 每日调用上限，默认 500 |
| `PORT` | 可选 | 云平台自动注入，`wsgi.py` 会读它 |

---

## 七、本机（不变）

本地开发/演示继续用原来的方式：

```bat
python run.py
```

浏览器打开 <http://127.0.0.1:8080>，需要公网就用 cpolar（面板 <http://127.0.0.1:9200>，当前隧道地址见 `公网地址.txt`）。

---

## 八、本次为部署新增/修改的文件

| 文件 | 作用 |
|---|---|
| `wsgi.py` | 云端 WSGI 入口，读 `$PORT`，并把工作目录钉到项目根（解决 `index.html` 相对路径） |
| `Procfile` | gunicorn 启动命令，`--workers 1` 保证异步进度不丢 |
| `render.yaml` | Render Blueprint，含中文字体安装与环境变量声明 |
| `Dockerfile` | 可选，给 Railway / Fly.io / 自建服务器用 |
| `vercel.json` + `api/index.py` | 让 Vercel 按 Python 构建（仅解决构建失败，不解决运行时） |
| `requirements.txt` | 去掉 BOM，补 `python-dotenv` / `Pillow` / `gunicorn` |
| `chart_renderer.py` | 中文字体候选扩展到 macOS + Linux（Noto CJK / 文泉驿），并加缺失告警 |
