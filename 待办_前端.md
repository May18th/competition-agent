# 前端待办清单（负责人：豆包 + 阿渡）

> 目标文件只有一份：**`C:\Users\34984\Doubao\chats\2026-09-23\new-chat\科创赛事助手\index.html`**
> `index.html` 由路由每次请求直读（`app.py` 第 840 行 `open('index.html')`），**改完刷新浏览器即生效，不用重启服务**。
> ⚠️ `app.py` 里还嵌着一份一模一样的旧 HTML 副本，**那是失效副本，改它页面不会有任何变化**，别动。

## 一、已完成（2026-09-25 05:00 前，不用再改）

| 项 | 内容 | 验证方式 |
|---|---|---|
| 图表渲染 | 三个图表容器终于有 `echarts.init + setOption` | 页面源码 `echarts.init` = 3 |
| 重复生成 | 加 `getInstanceByDom` 销毁旧实例，避免重复 init | `getInstanceByDom` 出现 4 次 |
| 自适应 | 两处 `1fr 1fr` 改 `repeat(auto-fit, minmax(280px,1fr))` | `auto-fit` = 2，旧写法 = 0 |
| 窗口缩放 | resize 监听只注册一次（原来每次生成叠加一个） | `__chartsResizeBound` = 2 |
| 重复 viewport | 两条 meta 去重 | `name="viewport"` = 1 |
| 键盘焦点 | 去掉 `outline:none`，补 `:focus-visible` | `focus-visible` = 1 |
| 性能 | 删掉 `* { transition: all 0.3s ease }`，短输入框降到 80px | 全局过渡 = 0 |
| CDN 兜底 | 新增 `static/marked.min.js`、`static/echarts.min.js`，CDN 挂了自动回落本地 | 页面含 `/static/marked.min.js` |
| 图表示例数据 | 图表区顶部加黄色提示条：数值为占位示例，与用户项目无关 | 提示文案出现 1 次 |

改完记得跑一次语法自检：把内联 `<script>` 抽出来用 `node --check` 跑一遍，目前是通过的。

## 二、待办（按优先级）

### P0｜先修复：复制/导出按钮点了没反应
- `copyText()` 调 `navigator.clipboard` 在非HTTPS/本地非安全上下文会静默失败，需要加降级方案：创建隐藏textarea选中文本execCommand('copy')
- `exportOne()` / `exportAll()` fetch没加错误捕获，后端接口报错时按钮看起来没反应，要加try/catch和loading态，失败弹提示
- 位置：1069-1109行
- 分工：WorkBuddy先改copyText降级，豆包改exportOne/exportAll的错误捕获

### P1.5｜接入后端新增的两个接口
1. 生成完成后读 `data.completeness`：如果 `missing` 非空，在结果区顶部显示黄色提示条「⚠️ 申报书缺少以下章节：xxx，建议补全后导出」
2. 页面初始化时请求 `/api/knowledge_status`，在输入区右上角显示小标签「知识库覆盖 4/11」，鼠标hover显示缺失赛事列表
3. 这两个是只读提示，不影响生成流程，接入不复杂

位置：第 733-739 行

```js
progress += 5;              // 每 500ms +5%，跟真实进度毫无关系
if (progress > 95) progress = 95;
```

问题：
1. 深度版要串行跑十来次模型调用，真实耗时 1-3 分钟，进度条 10 秒就顶到 95% 然后干等。
2. 整个请求**没有超时中断**（没有 `AbortController`），后端如果卡住，页面永远停在 95%，用户只能关标签页。

建议改法：
- 加 `AbortController`，5 分钟未返回就中断并提示「生成超时，请重试或改用简洁版」。
- 进度条要么接后端阶段事件，要么在文案里明说「进度均为示意，实际以生成完成为准」，不要造假进度。

### P2｜简洁版下 Tab 直接消失，没有任何解释

第 475-476 行：`chartsTab`、`defenseTab` 初始 `display:none`，只有深度版才显示。
但首页「Agent 流程条」宣传的是：规则解析 / 同质化检测 / 申报书生成 / 模拟评委 / 答辩预测 / PPT 大纲——**后两项简洁版根本不产出**。

建议二选一：
- A：在 Tab 上挂个「深度版专属」角标，简洁版用户至少知道为什么没有；
- B：把首页流程条按模式区分，简洁版只显示前四项。

### P3｜111 处内联 style，后续响应式改动很难做

第 8 行统计：全文 111 个 `style="..."`。类选择器管不到内联样式，媒体查询对它们无效（这也是之前响应式失效的根因）。
建议中长期：重复出现 3 次以上的模式抽成类，不要新增内联样式。

### P4｜移动端需要真机/窄屏验收
- 分工：
  - 豆包：补全375px窄屏媒体查询，版本选择卡自动堆成单列，按钮宽度100%，图表容器不横向溢出
  - WorkBuddy：把select下拉框在手机上宽度改成100%，检查内联style里固定宽度在窄屏下溢出的问题
- 验收标准：浏览器拖到375px宽，所有内容不横向滚动，按钮够大方便手指点

## 三、边界

- **别改 `app.py` 和 `competition_agents.py`**（Codex 在改）。
- 需要后端配合的地方写进 `待办_后端.md`，不要自己动后端代码。
- 项目目录里那 30 个 `fix_*.py` 是 Codex 写的补丁脚本，**已经执行过的（fix_charts / fix_responsive / fix_perf）不要重复跑**，它们会打印 ✅ 但部分替换为静默失败，重复跑也无害但没意义。
