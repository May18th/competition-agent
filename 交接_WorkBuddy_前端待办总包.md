# 交接给 WorkBuddy：前端待办总包（2026-09-26 汇总）

> 负责人：WorkBuddy（前端 + 部署）
> 后端（Codex）已定稿，无需再动 `app.py` / `competition_agents.py`。

---

## 〇、状态说明（先说已完成的，避免重复劳动）

以下**已完成并提交**（commit `53b92b4`），不用再做：

- ✅ 社会价值与应用前景 渲染（`social_value` → `#socialValue`）
- ✅ 项目简介 渲染（`project_summary` → `#projectSummary`）
- ✅ 图表点击放大
- ✅ 两档规格区分（后端侧）

---

## 一、剩余前端任务（按优先级）

### T1（推荐）：结果区顶部加档位标识

生成完成后，在结果区最上方显示一个标签：

- 简洁版 → 「简洁快速版」（灰色标签）
- 深度版 → 「深度完整版 · 已生成 N 个模块」（主题色标签）

位置：`index.html` 渲染结果处（`const isDeep = mode === 'deep'` 附近）。当前渲染区还没有这个标识。

### T2（推荐）：深度版专属板块加视觉区分

7 个深度版专属产物（技术方案 / 竞品分析 / 商业模式 / 风险分析 / 实施计划 / 社会价值 / 项目简介）显示时，给它们的容器加一个醒目边框或浅色背景，让用户一眼看出「这些是深度版才有的内容」。

位置：`document.querySelectorAll('.deep-only').forEach(...)` 那段，显示时额外加个高亮 class 即可。

### T3（可选，似乎已部分实现）：共享产物标字数

共享产物（规则解析 / 评委意见 / 快速诊断等）标题旁标字数，让深度版「内容更长」直观可见。若已实现可跳过。

---

## 二、卡片文案实测对齐（T4）

两档卡片上的耗时文案，请**真跑一次计时**后核对，不要凭感觉写：

- 简洁版：卡片写「约20秒 / 约30秒」→ 用实测值替换
- 深度版：卡片写「约1分钟 / 约1-2分钟」→ 用实测值替换

---

## 三、部署到阿里云（前端改完 + 后端已提交后）

等后端代码 push 到 GitHub 后，在阿里云服务器执行：

```bash
cd /opt/comp-agent && git pull && systemctl restart comp-agent
```

验证：`curl -s http://127.0.0.1:8080/api/health` 返回 `ok`，再公网访问 `http://8.149.236.90/`。

---

## 约束

- 只改 `index.html` / `static/`（前端），不要动 `app.py` / `competition_agents.py` / `docx_render.py` / `docx_inplace.py`。
- 卡片耗时文案必须先实测，再改。
