# 待办（Codex）：以官方为准 —— 34 条赛道待核 + 4 项存量

用户原话：「**反正每个说明都尽量以官方为主**」。
这条现在是硬约束，不是建议。

## 现状：我做了来源分级，结果是 34 : 3

索引里每条赛道现在带 `src` 字段，前端会显示徽章：
`官方原文`（绿）/ `知识库整理`（灰）/ `建议`（橙）。

当前 37 条赛道的分布：

```
official  3 条  ← 挑战杯三类（我查了官方章程第十九条 + 4 所高校校赛通知互证）
kb       34 条  ← 其余全部，来自知识库转述，未逐字核对
```

**34 条灰标，就是你的活。** 灰标意思是「我们不确定这是官方原话」，
学生会看到这个标记——不核就一直灰着，等于明说我们没底气。

## 任务 1（P0）：把 34 条赛道描述核成官方原文

做法：查官方章程 / 官方通知 / 赛事官网，逐条核对，改 `data/*.txt` 的 `## 赛道设置` 块。

**核对完，在 `scripts/build_comp_index.py` 的 `TRACK_SRC` 里把该赛事标成 `official`**，
否则前端仍显示灰标：

```python
TRACK_SRC = {
    "挑战杯": "official",   # 已核对：官方章程第十九条
    "iCAN": "official",     # ← 你核完加这里
}
```

优先级（按差异硬度，硬的先核）：

| 赛事 | 赛道 | 为什么先核 |
|---|---|---|
| 小挑 | 甲类（已注册企业）/ 乙类（创意项目） | 甲类要真实经营数据与财务，乙类要可行性论证 —— 写错直接偏题 |
| 国创 | 创新训练 / 创业训练 / 创业实践 | 结题材料完全不同（成果 / 商业计划 / 运营报告） |
| 信息安全 | 作品赛 / 能力赛（CTF） | **能力赛根本不写申报书**，选错全篇白写 |
| 广告艺术 | 平面 / 视频 / 动画 / 互动 / 广播 / 策划案 / 文案 | 作品形态完全不同 |
| iCAN | 创新 / 创业 / 挑战 | 主力赛事，创新赛道双盲评审泄露学校信息直接扣分 |
| 互联网+ | 高教主赛道 / 红旅 / 职教 / 产业命题 | 红旅看社会价值，主赛道看商业，差异明显 |
| 计算机设计 | 11 大类 | 数量多，逐个核；核不完至少核 AI 应用、软件应用两个主力 |
| 服务外包 | 企业命题 / 自主命题 | 差异中等 |

**一条硬规定**：核不到官方原文的，就让它继续灰着，
**不要把它写成像是官方的**。灰标不可耻，假装官方才可耻。

## 任务 2（P0）：真实获奖范文入库 —— 仍然是 0 条

这条挂了很久，再说一次：RAG 里 `kb_samples` 只有 13 条 AI 自撰样本
（`source="示范样本（AI 撰写，非真实获奖稿）"`，每条 399~565 字），**真实获奖稿 0 条**。
生成质量的天花板就卡在这里。

**入库坑（同根因，必须一起修）**：`_record_upload` 用 `_assess_and_summarize` 启发式打分，
**<75 分归「材料」，而 RAG 只取 `types=["范文"]`** → 真实获奖稿可能永远进不了 prompt，
跟「上传官方资料 100% 不参与生成」是同一个根因。需要专用入库路径或放宽判分。

## 任务 3（P1）：国创知识库仍然是空的

`data/国创.txt` 解析结果：**评分 0 项、章节 0 条**（15 个赛事里唯一一个双 0）。
上次列的标题错位（「## 立项标准」写的是选题要求、「## 结题要求」写的是结题材料）
还没修。学生选国创，要求卡会显示「缺：评分标准、章节要求」。

## 任务 4（P1）：动态索引接口，别让我每次重跑脚本

现在流程是：你改 `data/*.txt` → 我本地跑 `scripts/build_comp_index.py` → scp 到服务器。
**你改了我不知道，就会漏。**

建议加 `GET /api/competition/index`，直接读 `data/*.txt` 实时返回同结构 JSON。
前端改成：优先调接口，失败回落静态 `static/competition_index.json`。
这样你改完 txt 立即生效，不用等我。

## 不用做的（说清楚，省得重复）

- **真查重**：没有知网接口，做不了。靠「去 AI 味」档位 + 明确风险提示。
- **虚构专家身份**：已作废（虚构「某高校副教授」是新的诚信风险）。
- **蓝桥杯 / 西门子杯赛道**：差异太小（都是个人赛算法题 / 同套评分），
  做成选项点了也没变化，不如不做。
- **数学建模 / 电子设计赛道**：4 天 3 夜命题赛，客观上不存在赛道。

## 我这边的边界

`index.html` 归我，我不碰 `*.py`；`data/*.txt` 归你，但我这次改了
`挑战杯.txt`（换成官方原文）和 5 个文件的 `## 赛道设置`（小挑 / 国创 / 信息安全 /
广告艺术 / 服务外包，内容全部取自知识库已有事实，没有新增推断）。
如果你觉得越界了说一声，下次我走交接。
