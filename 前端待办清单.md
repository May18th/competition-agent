# 前端待办清单（Codex 后端 → WorkBuddy / 豆包）

> 汇总这一轮后端新增/改动的、需要前端接入的接口与字段。
> 详细接口说明见：`协作_PPT多风格主题.md`、`前端接口说明_知识库与章节校验.md`。

## 一、待接入清单（按优先级）

| # | 功能 | 接口/字段 | 前端要做的 | 文档 |
|---|---|---|---|---|
| 1 | PPT 多风格主题 | `GET /api/ppt_themes`，生成/导出传 `theme`、`variant` | 主题选择下拉框 + 传参 | 协作_PPT多风格主题.md |
| 2 | 知识库状态 | `GET /api/knowledge_status` | 知识库面板（覆盖率/缺失/格式告警） | 前端接口说明 §1 |
| 3 | 章节完整性 | 生成结果 `data.completeness` | 缺章节标红提示 | 前端接口说明 §2 |
| 4 | 今日额度 | `GET /api/quota` | 展示今日已用/上限，剩余 <20% 标黄 | 前端接口说明 §4 |
| 5 | 简洁版可选迭代 | 生成传 `iterate: true` | 加「低分自动优化」开关 + 说明文案 | 前端接口说明 §6 |
| 6 | 健康检查 | `GET /api/health` | 服务状态指示器（可选） | 本文 §二 |
| 7 | 历史分页/搜索/删除 | `GET /api/history`、`DELETE /api/history/<id>` | 历史列表分页 + 搜索框 + 删除按钮 | 本文 §三 |

## 二、健康检查接口 `GET /api/health`

返回服务状态，前端可在页面角落放一个「服务正常 / 异常」指示器。

```json
{
  "success": true,
  "status": "ok",
  "uptime_seconds": 3600,
  "knowledge_count": 4,
  "quota": {"calls": 42, "limit": 500, "remaining": 458, "exceeded": false},
  "history_count": 20,
  "last_generation_time": "2026-09-25 14:17"
}
```

## 三、历史记录接口（已迁 SQLite）

`GET /api/history?limit=20&offset=0&q=关键词`

```json
{
  "success": true,
  "history": [{"id": 20, "time": "...", "competition_name": "...", "idea": "...", "score": 83}],
  "total": 20,
  "limit": 20,
  "offset": 0
}
```

- `limit` / `offset`：分页
- `q`：按赛事名或创意关键词搜索
- `DELETE /api/history/<id>`：删除单条

## 四、注意

- 所有改动后端都已验证，前端接入后需重启 Flask 联调。
- 主题、知识库、章节、配额、迭代的详细说明已分散在 `协作_PPT多风格主题.md` 和 `前端接口说明_知识库与章节校验.md`，本文只是总览。
