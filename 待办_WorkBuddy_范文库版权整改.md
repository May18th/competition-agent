# 待办（WorkBuddy）：范文库版权整改 · 前端配合

## 结论

后台数据已整改完成：**已删除全部 92 条「范文」**（含公开获奖报道归纳 + 3 条真实获奖作品原文），只保留 1 条「材料」（官方资料）。种子文件已清空。前端需要同步把「范文库」这个概念从前端移除，避免用户再走「上传范文」这条链路。

## 版权原则（请写进前端提示）

- 官方通知、评分标准、空白模板 → 可收集，可保留。
- 别人的获奖申报书原文 / 商业计划书全文 → **禁止上传公网**，除非取得原作者书面授权。
- 网站定位是「AI 生成内容」，不是「范本库」。

## 前端要改的（index.html 归你，你改）

1. **上传入口**：去掉「上传范文 / 范文入库」选项，或改成「上传官方资料（规则/评分/模板）」。后端 `purpose=fanwen` 现在已不再入库为范文，只按「材料」收，但前端别再引导用户传范文。
2. **范文库面板**：如果首页/后台有「范文库」Tab 或「范文列表」，改成「官方资料库」，只展示「材料/模板」，不再展示「范文」。
3. **上传提示文案**：加一句「仅支持官方公开规则、评分标准、空白模板；他人原创申报书/计划书需原作者授权后方可收录」。
4. **RAG/生成结果引用**：如果前端有展示「参考了 X 篇范文」之类的字样，去掉或改成「参考了官方赛事规则」。

## 后端已完成（Codex，无需你动）

- `kb_samples` 里 type='范文' 的 92 条已删除，只剩「材料」。
- `real_winning_samples.json` / `seed_samples.json` 已清空。
- `_record_upload` 不再自动把上传内容归为「范文」，一律按「材料」。
- `_ref_block` 的范文 RAG 检索现在返回空（无范文可注入），生成不再参考任何获奖原文。

## 顺带（之前移动端卡顿那轮，仍在你这边）

- `/api/status` 轮询接 `cursor` 增量（后端已支持）。
- 结果页图表懒加载（滚动到视口再 init）。
- ECharts/marked 延迟加载。

