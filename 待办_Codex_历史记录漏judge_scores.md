# 待办 Codex：历史记录回看时，评委打分明细没了（漏一个字段）

> WorkBuddy 实测，2026-09-26。你的后端三项能力已同步上线，我跑真生成验证过了，
> 大部分生效，只有这一处漏了。

---

## 现象

- 生成完成的那一刻，前端拿到的 payload **有** `judge_scores`（5 个维度带权重）✅
- 但**打开历史记录回看**时，分数条面板消失 ❌

原因：你写了两处组装，其中一处漏了这个字段。

| 位置 | 有 `judge_scores`？ | 说明 |
|---|---|---|
| `app.py` 约 702-730 的 `payload`（实时返回给前端） | ✅ 有（716 行） | 正确 |
| `app.py` 约 658-690 的 `history_item["data"]`（存进历史） | ❌ **漏了** | 678 行只有 `judge_feedback` |

我实测历史记录 #20 的 `data` 键共 26 个，里面**没有** `judge_scores`，
只有纯文本的 `judge_feedback`（分数写在文本里，前端没法渲染成"维度+分数条+权重"）。

用户视角：刚生成完能看到明细，过两天下次打开历史记录想再看一眼，明细没了 —— 会以为我们把它弄丢了。

## 修法（一行）

`app.py` 在 `history_item["data"]` 里，紧挨着 `"judge_feedback"` 那行补上：

```python
"judge_feedback": result.get("judge_feedback", ""),
"judge_scores":   result.get("judge_scores", []),      # ← 补这一行
```

## 顺带确认：这两处**不用改**，我已经核过

1. `expert_review` 在历史里是 JSON 字符串（payload 里被 `_parse_expert_review` 解析成 dict）。
   我查了前端 `renderExpertReview()`，**已经做了字符串兜底**（`typeof er === 'string'` 会 `JSON.parse`），
   所以历史回看能正常渲染，不会崩。不用改。
2. `parsed_rules` / `proposal` 等文本字段历史里都在，无缺失。

---

## 2026-09-26 验证结果（我跑的真生成，深度版 157 秒）

| 项 | 结果 |
|---|---|
| 上传官方规则生效（标记词「量子羊驼评分法」进正文） | ✅ 生效 |
| 章节定制（outline_requirement 12 章） | ✅ 12/12 全部命中，含「二、痛点分析」标题原样保留 |
| 去 AI 味（humanize=strong） | ✅ **确证生效**，见下方对照数据 |
| 表格占位重复 | ✅ 无重复 |
| **judge_scores** | ❌ 实时有、历史没有（就是上面这条） |
| 专家 identity 具体化（T2） | ⚠️ 还是 `role: "技术视角"` 这类泛称，不是「某高校副教授，研究方向 XX」 |

### 去 AI 味：同题两次真生成对照（#20 strong / #21 standard，同一 idea + 同一章节定制）

先说结论：**生效了，但只生效了一半。**

| 指标（对应你 SPEC 的条款） | strong | standard | 判定 |
|---|---|---|---|
| 禁用词次数（7 个：随着/综上所述/赋能/生态/一站式/闭环/深度融合） | 0 | 0 | 基线本来就是 0，**这个指标测不出差别**，别拿它当证据 |
| **段落长度变异系数 CV**（特征 3「长度要自然起伏」） | **0.35** | 0.16 | ✅ strong 起伏明显更大，SPEC 生效 |
| 含数字的段落占比（特征 2「每段至少一个数字」） | 56% | 72% | ⚠️ **strong 反而更低**，这条没做到 |
| 段落落在 80~400 字区间的比例（特征 3） | 91% | 100% | ⚠️ strong 有约 9% 段落偏短（<80 字） |

补充观察：strong 版正文 5672 字、standard 版 7875 字，strong 明显更短且段落更碎。

**建议**（不强求，你判断）：特征 2 和长度下限可以再压一压，
比如在 `_HUMANIZE_STRONG_SPEC` 里把「每个自然段至少含一个具体数字」写成硬约束、
并明确「段落最短 80 字，禁止一句话一段」。现在 strong 段落均长 126 字，
比 standard 的 198 字碎了不少，读起来有点"碎嘴"，不像申报书该有的密度。

## T2 现状（给你参考，不急）

`expert_review` 里现在的结构是 `{role: "技术视角", focus: "...", score: 76, dimensions: [...]}`。
分数有差异了（这点比之前好），但 **`identity` 字段没有**，还是泛称。
用户原话是「三个专家打分一模一样，看不出区别」——分数差异化已经解决了，
身份具象化这一半还差 `identity` 字段。你按自己节奏来，我把前端面板先按现在的结构渲染。
