# 交接：赛事资料索引（前端「选比赛→立刻看到官方资料」）

> 这条是**告知 + 待办**，不是让你改前端。前端已上线，你只要知道它依赖什么。

## 做了什么

之前选完比赛，用户要等生成完（1～2 分钟）才知道这个比赛的评分标准长什么样。
现在选完**立刻**显示：评分标准、申报书章节、评委偏好、常见扣分点。

实现方式：把 `data/*.txt`（15 个赛事知识库）解析成结构化 JSON，前端启动时拉一次并缓存，
之后选赛事纯本地查表，**0 延迟、不占生成时间**。

- 索引文件：`static/competition_index.json`（15 个赛事，26 KB）
- 生成脚本：`scripts/build_comp_index.py`（`python scripts/build_comp_index.py` 即可重跑）
- 前端消费：`wbMatchIndex()` 本地匹配 → 命中就填进「本赛事要求」卡

## ⚠️ 对你有影响的一条

**你改了 `data/*.txt` 之后，要重跑一次生成脚本**，否则前端显示的还是旧资料。

```bash
python scripts/build_comp_index.py
```

或者更好——**建议改成动态接口**，一劳永逸：

```
GET /api/competition/index
返回 { version, items: [{ key, title, keys, intro, doc_type, scoring, sections, preference, deduct, chars, gaps }] }
```

内容和现在的 JSON 一致就行（`scripts/build_comp_index.py` 里的字段就是契约）。
有了这个接口，我把前端改成「优先调接口，失败回落静态 JSON」，以后你改知识库不用再打招呼。

## 解析时做了可信度过滤（你补资料时不用管，但要知道）

知识库里有些块「标题和内容对不上」，我在解析时做了过滤，
**不像章节标题的条目会被丢掉**（长度 > 26 字、或含 `。；，：` 的一律丢弃）。

受影响的是这两个：

| 文件 | 现象 | 前端表现 |
|---|---|---|
| `iCAN.txt` | 「## 申报书章节」写的是三个赛道说明 | 章节解析为 0 条 → 卡片顶部提示「缺：章节要求」，实际用前端内置 9 章兜底 |
| `国创.txt` | 「## 评分标准」是选题要求、「## 申报书章节」是结题要求 | 评分、章节都解析为 0 条 → 提示「缺：评分标准/章节要求/常见扣分点」 |

**你按《材料搜集清单_给Codex.md》P1 把这两份补了之后，重跑脚本，前端自动就有，不用再改前端。**

## 已验证

实跑前端 `wbMatchIndex()` 真代码，15 个下拉赛事名全部命中正确的知识库：

```
iCAN / 互联网+ / 挑战杯(大挑) / 小挑 / 国创 / 数学建模 / 电子设计 / 蓝桥杯 /
计算机设计 / 广告艺术 / 服务外包 / 信息安全 / 西门子杯 / RoboMaster / 金砖
```

重点验了**大挑和小挑没有串台**（这是上一轮修的 bug，前端这次也得保证不串）：
- `挑战杯全国大学生课外学术科技作品竞赛` → `挑战杯.txt`（大挑）
- `挑战杯中国大学生创业计划竞赛（小挑）` → `小挑.txt`
- 只输「挑战杯」→ `挑战杯.txt`（大挑，合理默认）
- 不认识的比赛 → 不匹配，卡片提示「这个比赛我们还没收录官方资料」
