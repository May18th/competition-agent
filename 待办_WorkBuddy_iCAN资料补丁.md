# 待办 · WorkBuddy：iCAN 资料已按官方材料打补丁

## 已做（后端，Codex）

根据用户提供的《iCAN大学生创新创业大赛资料.docx》，已更新：

1. `data/iCAN.txt`
   - 补齐三条赛道（创新/创业/挑战）的官方规则与评分标准；
   - 补上 AI 应用创新挑战赛（挑战赛道，依托奈势 AI）的提交材料、格式限制、官方模板、评委偏好、常见扣分点、近三年获奖项目。
   - 关键格式限制：应用方案 PDF ≤20 页且 ≤50MB；项目简介 ≤300 字；演示视频 ≤3 分钟 1080P；展示 PPT ≤20 页；附加佐证 ≤100MB；AI 辅助生成内容须标注「AI 辅助制作」。

2. `data/competition_profiles.json` 的 iCAN 条目
   - `chapters` 换成 AI 挑战赛道官方 11 章模板：
     项目概述 / 痛点分析与需求调研 / 项目总体方案与系统架构 / AI核心方案 / 功能设计与交互流程 / 原型Demo演示与测试结果 / 应用场景与用户价值 / 创新点提炼 / 落地规划与应用前景 / 团队介绍 / 参考文献·附录（可选）
   - `scoring` 保持官方 30/30/20/10/10（创新性/技术实现/实用价值/用户体验/应用前景）。
   - `focus` 改为强调「AI 是核心驱动而非套壳 + 奈势 AI 平台深度开发 + 可演示 Demo + 量化数据」。
   - `word_requirement.official` 写入上面的官方格式限制。
   - `double_blind` 仍为 true：AI 挑战赛道官方其实允许学校/导师，但按用户此前「生成材料严禁院校和导师信息」的要求，系统仍默认脱敏。

## 需要你（前端）同步的两处

1. `EVENT_REQ` 里 iCAN 的 `sections` 和 `scoring` 已经过时：
   - 现在 `sections` 还是旧的 9 章通用结构，建议换成上面 11 章官方模板；
   - 现在 `scoring` 是 `创新性30/实用性25/技术难度20/团队展示15/社会价值10`，官方 AI 挑战赛道应为 `创新性30/技术实现30/实用价值20/用户体验10/应用前景10`。
2. 更省事的做法：前端赛事卡、章节编辑器优先读 `GET /api/competition_profiles`，失败再回退到 `EVENT_REQ`。以后这类资料更新只改后端 JSON，不用前端再同步一份。

## 备注

- 若用户后续走的是 iCAN 创新赛道（双盲、硬性实物原型），章节和双盲口径与 AI 挑战赛道不同，需要按赛道拆分 profile；当前默认按 AI 挑战赛道配置（本项目是 AI 工具）。
- 获奖项目只保留项目名与奖项，没写学校/学生姓名，避免生成材料带出身份信息。
