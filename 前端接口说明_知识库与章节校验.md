# 前端接口说明：知识库状态 + 章节完整性校验

> 后端（Codex）新增两个前端可用的能力，本文档说明接口与展示建议。

## 1. 知识库状态接口

`GET /api/knowledge_status`

返回比赛知识库收录状态 + 每个文件的格式校验结果。

```json
{
  "success": true,
  "loaded_count": 4,
  "loaded_names": ["iCAN大赛", "互联网+大赛", "国创项目", "挑战杯"],
  "coverage": "4/11",
  "missing": ["金砖", "数学建模", "电子设计", "西门子", "计算机设计", "RoboMaster", "信息安全"],
  "files": [
    {
      "name": "iCAN大赛",
      "ok": false,
      "missing": ["申报书章节", "常见扣分点"],
      "detail": {"比赛介绍": true, "评分标准": true, "申报书章节": false, "偏好方向": true, "常见扣分点": false},
      "chars": 198
    }
  ]
}
```

字段说明：
- `coverage`：已收录 / 预期赛事数
- `missing`：还没收录的赛事（字符串列表）
- `files[].missing`：该文件缺的关键章节（`ok` 为 true 表示格式完整）

## 2. 章节完整性校验

`POST /api/generate` 的返回 `data` 里新增 `completeness` 字段：

```json
{
  "success": true,
  "data": {
    "proposal": "...",
    "completeness": {
      "ok": true,
      "missing": [],
      "detail": {"项目背景": true, "解决方案": true, "技术路线": true, "核心创新点": true, "商业模式": true, "风险与应对": true, "社会价值": true},
      "chars": 2500
    }
  }
}
```

- `ok`：申报书是否覆盖全部核心章节
- `missing`：缺失的章节名（前端可标红提示用户补写）
- `detail`：每个章节是否覆盖的明细

## 3. 前端展示建议

1. 「知识库」面板：显示覆盖率、缺失赛事清单；每个已收录文件若有格式缺失，标黄/标红提示。
2. 生成结果页：若 `completeness.missing` 非空，显示「申报书缺少以下章节：xxx」，引导用户补全或重新生成。

## 4. API 配额接口

`GET /api/quota`

返回今日 LLM 调用额度使用情况，前端可在页面角落展示「今日已用 / 上限」。

```json
{
  "success": true,
  "date": "2026-09-25",
  "calls": 42,
  "limit": 500,
  "remaining": 458,
  "exceeded": false
}
```

- `calls`：今日已成功调用 LLM 的次数
- `limit`：每日上限（默认 500，可经环境变量 `QUOTA_DAILY_LIMIT` 调整）
- `remaining`：剩余可用次数
- `exceeded`：是否已超限（超限后 `/api/generate` 会返回友好错误「今日调用次数已达上限」）

展示建议：剩余量低于 20% 时前端标黄，等于 0 时标红并禁用生成按钮。

## 5. 注意

- 两个能力都是「只读/提示」，不阻断生成，前端可选接入。
- 改后端后需重启 Flask。
