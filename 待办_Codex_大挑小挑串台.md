# P0：选「大挑」生成出的是小挑的评分体系（实测证据确凿）

> 发现人：WorkBuddy｜发现方式：真跑三个赛事做差异化验收
> 优先级：**P0** —— 这条直接影响正确性，学生选大挑拿到小挑的评分标准，整篇申报书方向就错了

## 现象

输入赛事名 `挑战杯全国大学生课外学术科技作品竞赛`（**大挑**），
输出的 `parsed_rules` 开头是：

```
**挑战杯（小挑）评分标准与申报要求分析**
## 一、核心评分维度与权重拆解
小挑的评分体系由五个维度构成：创新意义占25%，实践过程、社会价值、发展前景各占20%，团队协作占15%……
```

对比知识库原文，可确认命中的是 `data/小挑.txt`，不是 `data/挑战杯.txt`：

| 文件 | 实际内容 |
|---|---|
| `data/小挑.txt` | `# "挑战杯" 中国大学生创业计划竞赛（小挑）`；创新意义 25%、实践过程 20%、社会价值 20%、发展前景 20%、团队协作 15% |
| `data/挑战杯.txt` | `# "挑战杯" 全国大学生课外学术科技作品竞赛（大挑）`；创新性 30%、可行性 30% |

**生成的正文里写的就是小挑那五项权重** —— 串得非常彻底，不是偶尔。

## 根因（两处叠加，缺一不可）

### 1. `_ALIAS_GROUPS` 把大挑和小挑归成了同一组

`competition_agents.py:141`

```python
_ALIAS_GROUPS = [
    ["国创", "国创项目", "国创计划", "大创", "国家级大学生创新创业训练计划"],
    ["挑战杯", "大挑", "小挑"],          # ← 问题在这
    ["互联网+", "互联网加", "互联网＋"],
]
```

大挑和小挑**不是同一个比赛的两种叫法**，是两个独立赛事：

| | 大挑 | 小挑 |
|---|---|---|
| 全称 | "挑战杯"全国大学生课外学术科技作品竞赛 | "挑战杯"中国大学生创业计划竞赛 |
| 提交物 | 学术论文 / 调查报告 / 科技发明作品 | 商业计划书 |
| 侧重 | 科研严谨性 | 商业落地潜力 |
| 评分 | 创新性 30%、可行性 30% … | 创新意义 25%、实践过程 20%、社会价值 20%、发展前景 20%、团队协作 15% |

它们只是**共享「挑战杯」这个品牌名**，评分体系完全不同，不能互为别名。

### 2. `get_competition_knowledge` 命中第一个就 return，且遍历顺序不确定

`competition_agents.py:171-181`

```python
for known_name, content in competition_knowledge.items():
    if _competition_match(known_name, competition_name):
        return f"\n【知识库中关于{known_name}的资料】\n{content}\n"   # ← 第一个匹配就走
```

`competition_knowledge` 是普通 dict，key 来自 `os.listdir()` 顺序（**不保证稳定**）。
配合上面第 1 点：大挑 query 对每个 challenge 系 key 都返回 True，
谁排在前面就命中谁 —— 这次是「小挑」排在前面，于是大挑拿到了小挑的资料。

这个 bug 还有个更隐蔽的后果：**换个环境/换个文件顺序，结果可能反转**（小挑拿到大挑的资料），
变成时好时坏的玄学问题，更难排查。

## 建议修法

### 必做

1. **拆开别名组**：把 `["挑战杯", "大挑", "小挑"]` 这整组删掉，
   或改成按「品牌名 ≠ 赛事名」处理 —— 「挑战杯」只是品牌前缀，不能单独作为匹配依据。
   三个 key（`挑战杯` / `大挑` / `小挑`）本身就能被 `known in query or query in known` 命中，
   不需要别名组帮忙。

2. **不要命中第一个就 return**：改成收集所有匹配项，按匹配度打分取最优。
   参考实现：

   ```python
   def get_competition_knowledge(competition_name):
       if not competition_knowledge:
           return ""
       scored = []
       for known_name, content in competition_knowledge.items():
           s = _match_score(known_name, competition_name)   # 0 = 不匹配
           if s > 0:
               scored.append((s, known_name, content))
       if not scored:
           return ""
       scored.sort(key=lambda x: -x[0])          # 分数相同时再按 key 长度稳定排序，消除 dict 顺序依赖
       best = scored[0]
       print(f"📚 知识库命中：{best[1]}（匹配度 {best[0]}），候选 {len(scored)} 个")
       ...
   ```

   建议的 `_match_score`：
   - 完全相等 / `known in query` → 100
   - 去掉通用词后 token 集合**完全相同** → 70
   - token 有交集 → 40 + 交集大小
   - 仅品牌名相同（如都含「挑战杯」但后缀不同）→ **不算匹配**

3. **加一条自检**：启动时如果两个 key 互为别名组成员但内容里评分维度不同，打一条 warning。
   或者直接跑一次 `_competition_match("小挑", "挑战杯全国大学生课外学术科技作品竞赛")` 断言为 False。

### 顺带

4. 命中了哪个知识库，**在 `parsed_rules` 开头或返回字段里带出来**（比如 `knowledge_hit: "小挑"`）。
   这样我验收时一眼能看出有没有串，不用靠读正文猜。

## 验收方式（我会真跑）

用下面三个赛事名各跑一次，**检查 `parsed_rules` 里的评分权重是否对上**：

| 输入赛事名 | 期望命中的知识库 | 期望出现的权重 |
|---|---|---|
| `挑战杯全国大学生课外学术科技作品竞赛` | `挑战杯.txt`（大挑） | 创新性 30%、可行性 30% |
| `挑战杯中国大学生创业计划竞赛（小挑）` | `小挑.txt` | 创新意义 25%、实践过程 20% |
| `西门子杯中国智能制造挑战赛` | `西门子杯.txt` | （已验证正确，作对照组） |

判定标准：**前两条的 `parsed_rules` 里不能出现对方的权重数字**，且不能出现对方的赛事定位描述
（大挑要说「学术论文/调查报告/科研严谨性」，小挑要说「商业计划书/商业落地潜力」）。

## 备注

同一轮验收里，西门子杯和 RoboMaster 都正确命中了自己的知识库，内容也很贴比赛
（西门子杯讲「纯软件、纯理论、纯消费端的项目在这里基本没有生存空间」；
RoboMaster 讲「赛前技术评审 + 现场机甲对抗」两段式赛制）。
**说明 12 个知识库本身质量是好的，问题只出在挑战杯这一组的别名配置上。**
